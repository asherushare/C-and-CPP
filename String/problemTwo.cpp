#include<iostream>
#include<string>
using namespace std;

int main(){
  string s;
  getline(cin,s);
  cout<<s<<endl;

  // int start=0;
  // int end=s.size()-1;
  int count=0;
  int num=0;

  cout<<"size of string without using s.size() is: ";
  for(int start=0;start<=s.size();start++){
    if()
    num=count;
    count++;
  }

  cout<<num<<endl;


  return 0;
}
